Place the provided preview image here:

- Save the image attached to the conversation as `sefia-preview.png`.
- Put it at `src/assets/sefia-preview.png`.

The site meta tags reference `https://www.sefia.ao/assets/sefia-preview.png` for Open Graph and Twitter previews.

If you want me to add the binary file, upload the image into the workspace or tell me to fetch it from a URL and I will add it automatically.
